#include <stdio.h>
/**
 * main - prints base 10 numbers
 *
 * Return: Always 0 (Succuss)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
